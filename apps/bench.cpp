#include <cstdio>

// Binary A — latency: replay a LOBSTER message file, time each apply(), report
// p50/p90/p99/p99.9/max per op + throughput. Scaffold: prints and exits.

int main(int argc, char** argv) {
  (void)argc; (void)argv;
  std::puts("lob bench — scaffold (no engine yet)");
  return 0;
}
