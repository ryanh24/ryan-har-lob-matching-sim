#include <cstdio>

// Binary B — correctness: replay a LOBSTER message file, snapshot the book per step,
// diff against the orderbook oracle, print PASS/FAIL. Scaffold: prints and exits.

int main(int argc, char** argv) {
  (void)argc; (void)argv;
  std::puts("lob verify — scaffold (no engine yet)");
  return 0;
}
